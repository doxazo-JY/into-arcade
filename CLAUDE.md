@AGENTS.md

# 2팀 실시간 배팅 게임 — 교회 수련회용

## 상태

핵심 게임 루프(방 생성 → 팀 링크 접속 → 배팅 라운드 → 승패 판정 → 다음 라운드), 이벤트 시스템(전체 점수 교환 / 배팅 배수 2종류), 되돌리기/기록, 전체 초기화, 최종 순위, 중계(관전자) 화면까지 구현 완료하고 Vercel에 배포함(2026-08-05, `betting-game-olive.vercel.app`). 원래 기획서는 Supabase Auth(익명 로그인) + RLS + SQL RPC 기반의 훨씬 정교한 권한 구조를 요구했지만, 실제로는 로그인이 아예 없는 Prisma 직접 연결 구조(자매 프로젝트 [Borderless](../Borderless)와 동일 패턴)로 전면 전환했다 — 자세한 경위는 [notes/26.08.04.md](notes/26.08.04.md) 참고. 이벤트 종류도 기획서엔 7가지가 있었지만 실제로 필요한 건 2가지뿐이라는 사용자 확인을 받아 대폭 축소했다([notes/26.08.05.md](notes/26.08.05.md) 참고). 진행 상황·의사결정 경위는 [notes/](notes/) 디렉터리에 날짜별로 기록되어 있고, 남은 작업은 [PROGRESS.md](PROGRESS.md) 참고.

**기획서([기획서.txt](기획서.txt))는 이 앱의 출발점이지만 더 이상 정확한 사양이 아니다.** 사용자가 세션 중 여러 항목을 명시적으로 뒤집었으니, 기획서와 아래 "핵심 구조"가 다르면 **아래 내용과 notes/를 우선**할 것.

## 한 줄 요약

진행자 1명(모바일)이 게임방을 만들면, 참가자들은 수련회 시작할 때 한 번 받은 고정 링크(1팀 접속/2팀 접속)로 그때그때 "현재 진행 중인 방"에 접속한다. 매 라운드 각 팀이 비공개로 포인트를 배팅하면 진행자가 승패를 판정해 이긴 팀이 포인트를 얻고 진 팀이 잃는 게임. 팀은 자기 팀 결과만 보고 상대 팀 상황은 알 수 없으며, 진행자는 자기 화면(`/admin/[code]/[token]`)에서 양 팀 현황을 실시간으로 다 보고 중간중간 알아서 안내한다.

## 핵심 구조 (변경 시 기획서보다 이 문서와 notes/를 갱신)

- **로그인/회원가입 없음.** 진행자는 게임방 생성 시 발급되는 `admin_token`이 포함된 비밀 링크(`/admin/[code]/[token]`)로 접속한다. 코드를 직접 입력하는 폼은 없음(있었다가 "굳이 필요한가" 피드백으로 제거). **팀 단일기기 강제 접속**(`Team.activeSessionToken` 기반, 같은 팀 링크를 다른 기기에서 못 열게 막는 것)도 만들었다가 "한 장소에 모여 진행하니 필요 없다"는 이유로 제거함 — 동시에 여러 기기가 같은 팀 링크로 들어가는 걸 막지 않는다. ⚠️ 이건 아래의 **"참가자 잠금"과 다른 기능**이니 헷갈리지 말 것 — 단일기기 강제는 "다른 기기의 접근 자체를 막는 것"(폐기됨), 참가자 잠금은 "이미 들어온 화면에서 뒤로가기로 못 나가게 막는 것"(구현됨).
- **"현재 진행 중인 방" = 가장 최근에 만든 방**(2026-08-06 변경, `src/lib/currentRoom.ts`의 `getCurrentRoom()` — `Room.createdAt` 내림차순 1건). 별도 상태 테이블 없이 정렬만으로 구현 — 방은 삭제되지 않고 계속 쌓이기만 하므로 충분함. 참가자용 화면(`/play/1`, `/play/2`)은 전부 이 헬퍼로 방을 찾고, **토큰이 아예 없다** — URL 자체가 고정이라 수련회 시작할 때 한 번만 공유하면 새 방을 만들어도 다시 배포할 필요가 없음. 보호 수단은 "참가자에게 공유되지 않는 URL"이라는 점뿐 — 오프라인 소규모 행사라 이 정도로 충분하다는 판단. `Team.accessToken` 필드는 이제 안 쓰지만(레거시), 공유 DB 스키마 변경은 신중해야 해서 컬럼 자체는 남겨둠.
- **과거 방을 다시 "현재"로 되돌리는 기능**(`relinkRoom`, `src/app/admin/actions.ts`)도 있음 — "현재"가 `createdAt` 정렬로만 정해지므로, 과거 방의 `createdAt`을 지금 시각으로 갱신하는 방식으로 스키마 변경 없이 구현. 부작용으로 그 방의 "생성 시각" 표시가 최초 생성이 아니라 마지막 연결 시각으로 바뀌는데, 거의 안 쓸 기능이라 감수하기로 함. `/admin/into`(과거 방 목록)과 개별 방 콘솔(`/admin/[code]/[token]`, 현재 방이 아닐 때) 양쪽에 버튼 있음. 방 삭제(`deleteRoom`)도 같은 파일에 있고, Team/Round relation이 전부 cascade라 방 하나 지우면 관련 기록이 통째로 지워짐.
- **화면은 4개**: 참가자 홈(`/`, 1팀 접속·2팀 접속 2개 버튼만), 진행자 홈(`/admin/into`, **참가자에게 공유 안 하는 비공개 진입점** — 새 게임방 만들기 + 과거 게임방 기록 목록), 진행자 화면(모바일, `/admin/[code]/[token]`, 토큰 기반), 1팀/2팀 화면(모바일, `/play/1`, `/play/2`, 항상 현재 방). 예전엔 방마다 새 토큰 링크를 만들어 참가자에게 재배포해야 했는데, "이걸 어디 뿌릴 게 아니라 우리끼리만 쓰는 임시 프로그램인데 굳이 필요한가"라는 피드백으로 전면 재설계함 — 자세한 경위는 [notes/26.08.06.md](notes/26.08.06.md) 참고. 중계(관전자) 화면(`/watch`)은 원래 있었으나 2026-08-10에 아예 없앰 — 진행자 화면이 이미 양 팀 점수·배팅 현황을 실시간으로 다 보여주므로 별도 TV 화면이 불필요하다는 판단, 대신 팀 화면(`/play/[teamNo]`)에서 상대 팀 결과 노출도 같이 제거해서 참가자는 자기 팀 상황만 알 수 있게 했다.
- **진행자 홈 진입 경로는 `/admin`이 아니라 `/admin/into`다**(2026-08-10 변경) — `/admin`은 아무나 짐작해서 찍어볼 수 있는 흔한 경로라, 개별 방 콘솔(`/admin/[code]/[token]`)처럼 토큰까진 아니어도 짐작하기 어려운 세그먼트(`into`, 청년부 이름이자 "인투오락실" 테마와 맞음)를 하나 더 요구하게 함. 같은 이유로 방 생성 화면도 `/new`가 아니라 `/admin/into/new`로 옮김. 여전히 진짜 인증이 아니라 난독화 수준의 보호라는 한계는 그대로 — URL이 새면 그대로 뚫림.
- **진행자 화면의 "홈으로"는 `/`가 아니라 `/admin/into`로 간다** — `/`는 이제 참가자용이라 진행자가 갈 곳이 아님. 개별 방 콘솔의 "접속 링크" 섹션은 제거함 — 참가자 링크가 고정 링크로 바뀌면서 방마다 따로 보여줄 게 없어졌고, 이 방으로 돌아오는 길도 `/admin/into` 기록 목록으로 충분해서 "본인용 북마크" 링크도 불필요해짐.
- **팀 이름은 진행자만 정한다(진행자 화면에 그대로 노출되는 값이라 참가자 링크처럼 무인증으로 아무나 상시 수정 가능하게 두면 방송 사고 위험).** 다만 방 생성 시(`/admin/into/new`) 진행자가 기본값("1팀"/"2팀")을 그대로 두면, 그 팀이 처음 `/play/[teamNo]`에 들어올 때 **딱 한 번** 이름을 입력하고 입장하는 화면(`JoinForm`)이 뜬다 — "1명이 폰으로 조작하니 입장할 때 이름 쓰는 게 자연스럽다"는 피드백 반영(2026-08-06). 이름이 기본값이 아니게 된 순간부터는 다시 안 물어봄(`setTeamName`, `team.name !== defaultName`이면 "이미 설정됨" 에러) — 스키마 변경 없이 기본값 여부로만 판단.
- **참가자 잠금**(`Room.participantsLocked`, 2026-08-06 추가된 컬럼): 진행자가 진행자 화면에서 켜고 끄는 토글(`ParticipantLockToggle`). 켜져 있는 동안 `/play/1`, `/play/2`(그리고 아직 입장 전이면 `JoinForm`도)에서 뒤로가기를 눌러도 그 화면을 못 벗어난다(`BackLock`, `history.pushState`로 popstate를 계속 무효화). **`/` 홈 화면(팀 선택 화면) 자체도 잠긴 동안은 못 씀** — 처음엔 뒤로가기만 막았는데 "주소창에 `/` 직접 치면 그냥 들어가진다"는 피드백을 받아, `/`을 열 때마다 서버에서 잠금 여부를 확인하고(`HomeGate`) 잠겨있으면 버튼 자체를 안 보여줌. 이 기기가 이미 어느 팀인지는 `localStorage`(`myTeamNo`, `RememberTeam`이 `/play/[teamNo]` 진입 시 기록)로 기억해뒀다가 있으면 그 팀 화면으로 즉시 리다이렉트하고, 처음 보는 기기면 "잠겨 있다"는 안내만 보여줌 — 세션/로그인 없이 클라이언트 기억만으로 구현. 시크릿창이나 다른 기기로 우회하는 것까지는 안 막아도 된다는 전제(사용자 확인).
- **기술 스택**: Next.js(App Router, TS) + Prisma + `@prisma/adapter-pg` + Supabase Postgres(Vercel 배포). 실시간성은 진짜 push가 아니라 클라이언트 폴링(`PollRefresh`, 1.5~2초 간격)으로 구현. 2026-08-06부터 `visibilitychange`/`focus` 이벤트도 감지해서 탭이 다시 보이거나 창이 포커스를 받는 즉시 한 번 더 새로고침함 — 브라우저가 백그라운드 탭·비포커스 창의 `setInterval`을 강하게 스로틀링해서(배터리 절약용 기본 동작) interval만으로는 새로고침이 몇 분씩 늦어지는 문제가 실사용 테스트 중 발견됨.
- **런타임 DB 연결은 `DATABASE_URL`(6543, pgbouncer=true, 트랜잭션 모드 풀링)을 쓴다**(2026-08-06 변경, `src/lib/prisma.ts`). 예전엔 "direct(5432)만 쓴다"고 되어 있었는데, 실제 테스트 중 `EMAXCONNSESSION: max clients reached in session mode` 에러가 재현됨 — `DIRECT_URL`(5432)이 이름과 달리 실제로는 Supabase의 세션 모드 풀러(Supavisor)를 거치고 있었고, Next.js 라우트마다 뜨는 서버리스 인스턴스가 각자 `PrismaPg`로 자체 `pg.Pool`(기본 max 10)을 새로 만드는 구조라 사용자 수가 적어도 인스턴스 몇 개만 겹치면 세션 모드의 낮은 pool_size(예: 15)를 쉽게 넘김. 트랜잭션 모드는 인스턴스가 몇 개 뜨든 전부 Supavisor라는 중앙 풀러의 클라이언트가 되는 구조라 실제 Postgres 백엔드 연결 수를 풀러가 중앙에서 제한함 — 자매 프로젝트 Borderless도 동일 방식(`DATABASE_URL`, 6543)을 씀. 마이그레이션(`prisma.config.ts`)은 여전히 `DIRECT_URL`(5432)을 씀 — 트랜잭션 모드 풀링은 마이그레이션에 안 맞음. ⚠️ 실제 다수 기기 동시 접속 부하 테스트는 아직 못 해봄 — 행사 전 확인 필요.
- **개발 환경에서 Prisma 클라이언트를 `globalThis`에 캐싱하지 않는다**(`src/lib/prisma.ts`, 프로덕션만 캐싱) — 캐싱하면 스키마를 바꿔도 서버를 완전히 재시작하기 전까진 반영이 안 됨.
- **게임 상태를 보여주는 페이지는 전부 `export const dynamic = "force-dynamic"`을 명시**(`/admin/into`, `/admin/[code]/[token]`, `/play`) — Vercel 프로덕션에서 이 설정 없이 캐싱되어 폴링 새로고침이 오래된 화면을 계속 보여주는 버그가 있었음.
- **이벤트는 딱 2종류뿐**: 전체 점수 교환(`SWAP_ALL`), 배팅 배수(`BET_MULTIPLIER`). 둘 다 대상은 항상 양 팀, 공개 방식은 항상 전체 공개 — 대상 팀 선택이나 공개 방식 선택 UI가 없음. **둘 다 라운드가 WAITING(시작 전) 상태일 때만 실행 가능** — BETTING이 시작되면 버튼 자체가 안 보임.
- **배팅 배수 계산은 대칭**: 이긴 팀은 `배팅액 × 배수`를 얻고, 진 팀도 동일하게 `배팅액 × 배수`를 잃는다(`applyRoundResult`, `ResultForm`, `roundHistory.ts`) — 처음엔 진 팀은 배수 무관하게 배팅액만 잃도록 만들었으나, 2026-08-05에 "진 팀도 배수만큼 잃어야 한다"는 피드백으로 대칭 계산으로 변경.
- **배팅 금액 공개 시점**: 라운드가 끝나기 전(WAITING/BETTING)에는 상대 팀 배팅 금액이 어디에도 노출되면 안 된다. 2026-08-10 이전에는 라운드가 RESOLVED된 뒤 팀 화면에 상대 팀 결과 카드를 보여주고 중계 화면(`/watch`)에도 공개했으나, "상대 팀 상황은 몰라야 배팅 게임의 긴장감이 유지된다"는 피드백으로 **팀 화면(`/play/[teamNo]`)에서는 라운드가 끝난 뒤에도 상대 팀 결과를 영영 노출하지 않는 것으로 바뀜** — 자기 팀 결과·점수는 그대로 보여줌. 게임 완전 종료 시엔 양 팀 모두에게 라운드별 전체 내역표(`RoundHistoryTable`)를 보여주는데, 이건 게임이 끝난 뒤라 예외로 둠. 진행자 화면은 계속 예외로, 확정되는 즉시(라운드 중이라도) 양 팀 배팅 금액이 다 보인다(진행자 개인 기기라 확인 절차가 불필요한 마찰이라는 판단이고, 진행자가 중간중간 상황을 파악해 진행하는 데 필요함).
- **"진행 중" 배너는 배팅 배수에만 쓴다.** 전체 점수 교환은 실행되는 순간 이미 끝나는 이벤트라 계속 표시하면 안 됨(`getActiveMultiplierEvent` 헬퍼, 진행자·팀 화면 공통 사용) — 이벤트 실행 자체는 확인창/팝업으로만 1회 안내.
- **"두 팀 다 배팅 확정 → 진행자 판정 대기" 상태는 `GameInProgressBadge`(`src/components/`) 공용 컴포넌트로 통일**(2026-08-06, 현재는 팀 화면에서만 사용) — 게임 종료 후 라운드별 내역표(`RoundHistoryTable`)에도 이벤트를 같이 표시함(2026-08-06) — 배팅 배수는 `Round.multiplier`가 이미 그 정보라 별도 조회 없이 라운드 옆에 텍스트로("R4 (×2)"), 전체 점수 교환은 `EventLog.roundId`로 판단해 해당 라운드 앞에 흐린 텍스트 한 줄로("전체 점수 교환") — 둘 다 뱃지/이모지 없이 텍스트로만(장식하면 "AI가 붙인 티" 난다는 반복된 피드백 반영).
- **전체 초기화 버튼은 진행자 메인 화면이 아니라 "기록" 페이지(`/admin/[code]/[token]/history`) 안에 있다** — 게임 중 실수로 누를 위험을 줄이기 위해 일부러 덜 눈에 띄는 곳으로 옮김. 이중 확인 + "초기화" 텍스트 직접 입력 필요. 게임 종료 버튼은 파괴적이지 않아 메인 화면에 그대로 있음.
- 점수 변경은 전부 `ScoreTransaction` 원장 테이블에 기록된다. **되돌리기(revert) 기능은 배팅 배수 이벤트 취소(`undoEvent`, 진행자 메인 화면의 "이 이벤트 취소") 하나만 남아있다** — 2026-08-11에 라운드 결과·점수 직접 수정 되돌리기(`undoRoundResult`/`undoManualAdjust`)를 제거했다. 이유: `ResultForm`처럼 적용 전에 "이 결과를 적용하시겠습니까?" 확인 화면을 거치면 사후 되돌리기가 굳이 필요 없다는 판단 — 그래서 "점수 직접 수정"도 같은 확인 단계를 추가하고, 사후 되돌리기 대신 **적용 전 확인**으로 안전장치를 옮겼다. 남은 `undoEvent`는 값을 직접 되돌리는 게 아니라 보정 트랜잭션을 추가로 쌓는 방식(이후 다른 변경이 있었으면 경고 후 강제 진행 옵션)을 그대로 씀. 페이지 이름도 "기록/되돌리기"에서 "기록/점수 수동 수정"으로 바꿈 — 되돌리기 버튼은 없지만 점수를 고칠 수 있다는 걸 이름에서부터 드러내기 위함. **게임 종료(`Room.status === "ENDED"`) 후에는 점수 직접 수정 자체가 막힌다**(UI에서 폼을 안내 문구로 대체하고, `manualAdjustScores` 서버 액션에도 동일 가드) — 최종 순위 발표 후 실수로 점수가 바뀌는 사고를 막기 위함.
- **점수 직접 수정은 1팀/2팀을 한 번에 같이 조정할 수 있다**(`manualAdjustScores`, 2026-08-11) — 원래 팀을 하나씩 골라 따로 두 번 제출해야 했는데, "한 번에 양 팀 다 고치는 게 더 자연스럽다"는 피드백으로 두 팀 입력 칸을 나란히 두고 값을 입력한 팀만 골라 하나의 트랜잭션으로 함께 적용하도록 바꿨다(빈 칸은 "이 팀은 안 건드림"). **점수 직접 수정은 조작 시점의 "지금 라운드"(`Round.id`)에 묶여서, 게임 종료 후 라운드별 결과표(`RoundHistoryTable`)에 전체 점수 교환 이벤트와 똑같은 방식으로 표시된다** — 처음엔 별도 표로 분리했다가 "이벤트처럼 그 시점에 표시되는 게 낫다, 표를 굳이 하나 더 두지 말자"는 피드백으로 통합함(`getRoundHistory`가 각 라운드에 `manualAdjustsBefore`/`manualAdjustsAfter` 배열을 함께 반환 — 그 라운드의 결과 확정 시각(`RoundResult.appliedAt`) 전에 수정했으면 라운드 행 앞에, 후면 뒤에 넣는다. 결과 확정 전/후 구분 없이 무조건 "앞"에 넣었다가, "라운드 1 끝난 뒤에 수정했는데 앞에 나오는 게 이상하다"는 피드백으로 나눔). 라운드 결과가 하나도 없어도 그 라운드에 점수 직접 수정이 있으면 표에 포함된다. 팀 화면에도 게임 종료 후 예외로 공개되는 다른 라운드 기록과 동일하게 노출됨.
- **"승/패를 잘못 눌렀을 때"는 점수 직접 수정이 아니라 "결과 다시 정하기"(`CorrectResultButton`, `correctRoundResult`)를 쓴다**(2026-08-11) — 진행자 메인 화면에서 라운드가 RESOLVED 상태일 때(= "다음 라운드"를 아직 안 눌렀을 때만) "다음 라운드" 버튼 옆에 노출되고, 라운드가 넘어가면 이 버튼 자체가 사라져서 자연스럽게 "다음 라운드 시작 전까지만" 제약이 걸린다. 처음엔 예전처럼 라운드를 `RESULT_PENDING`으로 되돌리고 재판정하는 방식을 고려했으나, `RoundResult`가 `(roundId, teamId)` 유니크 제약이 있어서 되돌린(reverted) 행이 남은 채로 재판정 시 새로 `create`하면 충돌한다는 문제를 발견 — 대신 기존 `RoundResult` 행의 `outcome`을 그 자리에서 업데이트하고, 원래 배수·배팅액은 그대로 재사용해 차액(정정 후 delta − 정정 전 delta)만큼 보정 `ScoreTransaction` 하나를 추가하는 방식으로 구현. 중간 상태(`RESULT_PENDING`) 없이 라운드는 계속 RESOLVED 상태를 유지하고, 결과표엔 정정된 최종 결과만 깔끔하게 남는다.
- **서버 실행(`npm run dev`)과 `git push`는 절대 대신 실행하지 말 것.** 사용자가 세션 내내 여러 번 명시적으로 요구한 규칙 — 실행 가능한 명령은 코드블록으로만 안내.
- **디자인은 수련회 PPT("2026 인투 오락실")의 8비트 오락실 톤에 맞춘 레트로 시스템으로 전 화면 통일**(2026-08-05, `src/app/globals.css`) — 팀 컬러는 PPT에 없어 새로 정함: 1팀=레드(`team-red`)/2팀=블루(`team-blue`, 고전 2P 대전 관례), 승리=골드(`win`), 패배=차분한 슬레이트(`lose`, 팀 컬러와 안 겹치는 "게임오버" 톤), 이벤트=바이올렛(`event`). 다크모드는 안 씀(카지노/스포츠토토 연상을 피하려는 결정) — 대신 하늘·잔디·흙 배경(`.world-ground`, `.world-stars`)이 `layout.tsx`에서 뷰포트 바닥에 고정되어 모든 화면에 항상 깔림. 모서리는 항상 각지게(`rounded-*` 안 씀, 원형 점 표시 제외), 그림자는 블러 없는 계단식 오프셋(`.shadow-sticker`)으로 "픽셀" 질감을 낸다. 자세한 시안 검토 경위는 [notes/26.08.05.md](notes/26.08.05.md) 참고.
- **홈 화면(`/`) 타이틀·나무는 CSS 도형이 아니라 실제 이미지 자산**(2026-08-07, `public/인투오락실.png`, `public/tree-trace.png`) — 처음엔 한글 지원 도트 폰트(갈무리)와 SVG 곡선 텍스트, `clip-path`로 그린 픽셀 나무 캐노피(사각 블록 → 계단형 원 → 비대칭 실루엣 → 겹친 타원 블롭까지 5차례 이상 시도)로 만들었으나, 브라우저 렌더링을 직접 확인할 방법이 없는 채로 좌표를 손으로 계산해 보내는 방식이라 계속 미묘하게 어긋났다(검은 쐐기 무늬, 흙 구멍 텍스처 미표시, 나무가 공중에 뜨는 버그 등). 결국 사용자가 준 실제 이미지(타이틀 PNG)와 픽셀아트를 SVG로 트레이스한 파일을 Python/PIL로 배경만 투명 처리해 이미지 자산으로 대체 — 자세한 경위는 [notes/26.08.07.md](notes/26.08.07.md) 참고. 장식은 `.home-scene`/`.home-ground`(`src/components/SceneDecoration.tsx`, 클래스 이름은 `.home-*`로 남아있지만 이제 홈 전용 아님)에 모여 있고, 모든 화면 공통 배경(`.world-ground`, `.world-stars`, 30px 얇은 띠)과는 분리되어 있음 — 원래 홈 화면(`/`)에만 썼다가 참가자용 화면인 1팀/2팀 접속(`/play/[teamNo]`)과 당시 있던 중계 화면(`/watch`, 2026-08-10에 제거됨)에 적용했고, "진행자 화면만 장식이 빠져서 어색하다"는 피드백으로 진행자 쪽 화면(`/admin/into`, `/admin/[code]/[token]`, `/admin/[code]/[token]/history`)에도 마저 적용해 이제 전 화면 공통이다. 모바일 좁은 화면에서는 PC 기준 크기(땅 112px, 나무 최대 230px 등)가 과하게 커 보여서 `max-width: 640px` 미디어 쿼리로 축소함 — 땅은 내부 좌표를 다시 계산하지 않고 `scaleY(0.5)`로 세로만 줄이고, 나무는 `next/image`에 className만으로 폭 지정이 안 먹혀서 CSS 변수(`--tree-left-w` 등)를 인라인 `style`에서 `var()`로 읽는 방식으로 우회.

## 남은 것

기획서와의 차이는 [PROGRESS.md](PROGRESS.md) 참고. 대부분은 "2차 범위로 미룸"이 아니라 **하나씩 필요성을 확인한 뒤 "실제로는 불필요"로 결론난 것**들이다(게임 도중 설정 변경, 라운드 임의 이동, 다크모드 수동 토글, 효과음/진동, 애니메이션, 커스텀/랜덤 이벤트 등) — 새로 세션을 시작해서 이 항목들을 "미완성"으로 착각해 만들지 말 것.
